/// Shared active filters so the selection made on the Eats list also applies
/// to the Food Map ("filters set before").
class FilterState {
  static final Set<String> categories = {};
  static final Set<String> chains = {};
}
